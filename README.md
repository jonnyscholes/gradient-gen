Visual Gradient Generator - WIP
===============================

A way to easily and visually layer gradients together into a single background-image using `linear-gradient()` and `radial-gradient()`.

